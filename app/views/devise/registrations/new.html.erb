<div id="registrar">
	<h2>Registrarse</h2>

	<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
	  <%= devise_error_messages! %>

	  <div><%= f.label :email, "Correo Electronico" %><br />
	  <%= f.email_field :email, autofocus: true, placeholder: "Email" %></div>

	  <div><%= f.label :password, "Contraseña" %><br />
	    <%= f.password_field :password, autocomplete: "off", placeholder: "Password" %></div>

	  <div><%= f.label :password_confirmation, "Confirmar Contraseña" %><br />
	    <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Confirmar password" %></div>

	  <div><%= f.submit "Registrar" %></div>
	<% end %>

	<%= render "devise/shared/links" %>
</div>
